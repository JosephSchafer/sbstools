=Optimiziations=

  * check field types of database: Is it really necessary to use BIGINT for primary key fields?
  * skip some messages?

==Data flood==

  every day the database grows by ~100MB

  * reduce flight data by about 90% for flights not crossing Vorarlberg